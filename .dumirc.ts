import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  themeConfig: {
    name: 'tootl',
    nav:[
      { title: 'tootl', link: '/components/is-array' }
    ]
  },
});
